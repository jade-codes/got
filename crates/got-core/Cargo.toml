[package]
name = "got-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
faer = "0.19"
thiserror = "1"
sha2 = "0.10"

[dev-dependencies]
serde_json = "1"
