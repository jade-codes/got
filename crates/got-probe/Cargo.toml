[package]
name = "got-probe"
version = "0.1.0"
edition = "2021"

[dependencies]
got-core = { path = "../got-core" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
rand = "0.8"

[dev-dependencies]
serde_json = "1"
